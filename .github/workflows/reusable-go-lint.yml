# SPDX-FileCopyrightText: 2026 Naira Project Contributors
# SPDX-License-Identifier: Apache-2.0

name: Reusable · Go Lint

on:
  workflow_call:
    inputs:
      go-version:
        description: "Go version to use"
        type: string
        default: "1.22"
      working-directory:
        description: "Directory containing go.mod"
        type: string
        default: "."
      golangci-lint-version:
        description: "golangci-lint version"
        type: string
        default: "v1.57.2"
      timeout:
        description: "Lint timeout (e.g. 5m)"
        type: string
        default: "5m"
jobs:
  go-lint:
    name: Go Lint
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    defaults:
      run:
        working-directory: ${{ inputs.working-directory }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ inputs.go-version }}
          cache-dependency-path: ${{ inputs.working-directory }}/go.sum

      - name: Cache golangci-lint
        uses: actions/cache@v5
        with:
          path: ~/.cache/golangci-lint
          key: golangci-lint-${{ inputs.golangci-lint-version }}-${{ runner.os }}

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: ${{ inputs.golangci-lint-version }}
          working-directory: ${{ inputs.working-directory }}
          args: --timeout=${{ inputs.timeout }}
          # Disable golangci-lint's built-in caching — managed by actions/cache above
          skip-cache: true

      - name: Check go mod tidy
        run: |
          go mod tidy
          git diff --exit-code go.mod go.sum || \
            (echo "::error::go.mod or go.sum is not tidy. Run 'go mod tidy' and commit." && exit 1)
