# SPDX-FileCopyrightText: 2026 Naira Project Contributors
# SPDX-License-Identifier: Apache-2.0

name: Reusable · Node Lint

on:
  workflow_call:
    inputs:
      node-version:
        description: "Node.js version to use"
        type: string
        default: "20"
      working-directory:
        description: "Directory containing package.json"
        type: string
        default: "."
      package-manager:
        description: "Package manager: npm | yarn | pnpm"
        type: string
        default: "npm"
      lint-script:
        description: "package.json script name for linting"
        type: string
        default: "lint"
      typecheck-script:
        description: "package.json script for type checking (leave empty to skip)"
        type: string
        default: "typecheck"

jobs:
  node-lint:
    name: Node Lint & Typecheck
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    defaults:
      run:
        working-directory: ${{ inputs.working-directory }}

    steps:
      - name: Checkout code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false

      # Resolve the correct lockfile for the chosen package manager.
      # Each package manager uses a different lockfile name; using the wrong
      # one produces incorrect cache keys and stale caches.
      - name: Resolve lockfile path
        id: lockfile
        shell: bash
        run: |
          case "${INPUTS_PACKAGE_MANAGER}" in
            yarn) echo "path=${INPUTS_WORKING_DIRECTORY}/yarn.lock" >> "$GITHUB_OUTPUT" ;;
            pnpm) echo "path=${INPUTS_WORKING_DIRECTORY}/pnpm-lock.yaml" >> "$GITHUB_OUTPUT" ;;
            *)    echo "path=${INPUTS_WORKING_DIRECTORY}/package-lock.json" >> "$GITHUB_OUTPUT" ;;
          esac
        env:
          INPUTS_PACKAGE_MANAGER: ${{ inputs.package-manager }}
          INPUTS_WORKING_DIRECTORY: ${{ inputs.working-directory }}

      # ── pnpm must be on PATH before actions/setup-node when cache: pnpm ───
      # setup-node needs pnpm installed first to locate the lockfile for caching.
      - name: Setup pnpm
        if: inputs.package-manager == 'pnpm'
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9

      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ inputs.node-version }}
          cache: ${{ inputs.package-manager }}
          cache-dependency-path: ${{ steps.lockfile.outputs.path }}

      # ── npm ────────────────────────────────────────────────────────────────
      - name: Install dependencies (npm)
        if: inputs.package-manager == 'npm'
        run: npm ci

      # ── yarn ───────────────────────────────────────────────────────────────
      - name: Install dependencies (yarn)
        if: inputs.package-manager == 'yarn'
        run: yarn install --frozen-lockfile

      # ── pnpm ───────────────────────────────────────────────────────────────
      - name: Install dependencies (pnpm)
        if: inputs.package-manager == 'pnpm'
        run: pnpm install --frozen-lockfile

      # ── Lint ───────────────────────────────────────────────────────────────
      - name: Run ESLint
        run: |
          "${INPUTS_PACKAGE_MANAGER}" run "${INPUTS_LINT_SCRIPT}"
        env:
          INPUTS_PACKAGE_MANAGER: ${{ inputs.package-manager }}
          INPUTS_LINT_SCRIPT: ${{ inputs.lint-script }}

      - name: Run TypeScript typecheck
        if: inputs.typecheck-script != ''
        run: |
          "${INPUTS_PACKAGE_MANAGER}" run "${INPUTS_TYPECHECK_SCRIPT}"
        env:
          INPUTS_PACKAGE_MANAGER: ${{ inputs.package-manager }}
          INPUTS_TYPECHECK_SCRIPT: ${{ inputs.typecheck-script }}

      # ── Formatting (Prettier) ─────────────────────────────────────────────
      - name: Check Prettier formatting
        run: |
          if [ -f ".prettierrc" ] || [ -f ".prettierrc.json" ] || [ -f "prettier.config.js" ]; then
            "${INPUTS_PACKAGE_MANAGER}" run format:check 2>/dev/null || \
              echo "::warning::No format:check script found, skipping Prettier check."
          else
            echo "No Prettier config found, skipping."
          fi
        env:
          INPUTS_PACKAGE_MANAGER: ${{ inputs.package-manager }}
